    import XCTest
    @testable import SwipeChart

    final class SwipeChartTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwipeChart().text, "Hello, World!")
        }
    }
